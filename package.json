{
  "name": "topramenking",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "build": "CI=false && cd client && npm install && npm run build",
    "devBuild": "cd client && npm install && npm run devBuild",
    "start": "node server/server.js",
    "devstart": "nodemon server/server.js"
  },
  "engines": {
    "node": "v16.17.0"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.0.1",
    "dotenv": "^16.0.1",
    "express": "^4.18.1",
    "express-session": "^1.17.3",
    "mongoose": "^6.4.1",
    "multer": "^1.4.5-lts.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.18",
    "sass": "^1.54.0"
  }
}
